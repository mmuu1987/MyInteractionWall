//1 定义kernel的名称
#pragma kernel CSMain1
//2 定义buffer
RWTexture2D<float4>  Result;
//3 kernel函数
//组内三维线程数
[numthreads(8,8,1)]
void CSMain1 (uint3 id : SV_DispatchThreadID)
{
    //给buffer赋值
    //纯红色
    //Result[id.xy] = float4(1,0,0,1);
    //基于uv的x给颜色
    float v = id.x/256.0f;
    Result[id.xy] = float4(id.x/350.0f,id.y/256.0f,0,1);
}